{
  "name": "robin-command-room",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "check": "node scripts/verify-assets.mjs --self-test && node scripts/test-movement.mjs && node scripts/test-mission.mjs && node scripts/test-demo-pilot.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "7.3.6"
  }
}
